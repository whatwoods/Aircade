# Aircade 脚手架实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-04-17-scaffold-design.md` 把 Aircade 项目骨架建立起来——跑得起 `pnpm dev`、PRD §6 的 8 张表通过 drizzle migration 建到本地 Postgres、`pnpm lint` `pnpm typecheck` `pnpm test` `pnpm build` 全部通过，不含任何业务功能。

**Architecture:** Next.js 14 App Router（TS + Tailwind + shadcn/ui）单体；Drizzle ORM 直连本机 PostgreSQL；手写认证所需的依赖（`bcryptjs`、`jose`）和 Session/User 等表先备好但不写逻辑；`src/features/*` 按 feature 切分目录，`src/lib/*` 放无业务通用工具。

**Tech Stack:** pnpm · Next.js 14 · TypeScript · TailwindCSS · shadcn/ui · Drizzle ORM + postgres.js · ioredis · Vitest · ESLint · Prettier · Husky + lint-staged · zod

**与 spec §12 的一处偏离：** 本计划的 Task 1 会执行 `git init`。spec 原本说"本期不 init git，README 提醒用户自己做"——但后续任务每步都要 `git commit`（TDD/频繁提交工作流的一部分），没有仓库无法提交。若不希望初始化 git，请在开工前告知，可把所有 commit 步骤改为本地文件快照。

---

## 任务总览

| # | 任务 | 产出提交 |
|---|---|---|
| 1 | 初始化仓库与基础配置 | chore: init repo |
| 2 | 安装核心依赖 + Tailwind/PostCSS | chore: add next+tailwind baseline |
| 3 | Tailwind 主题 token（PRD §8 调性） | feat(theme): brand tokens |
| 4 | 字体（next/font） | feat(theme): fonts |
| 5 | 根 layout + 占位首页 | feat(app): baseline layout |
| 6 | Env 校验（TDD） | feat(lib): env validation |
| 7 | Vitest 接入 | chore: vitest setup |
| 8 | ESLint + Prettier | chore: eslint + prettier |
| 9 | Husky + lint-staged | chore: pre-commit hooks |
| 10 | Redis 客户端 | feat(lib): redis singleton |
| 11 | Drizzle 配置 + db client | feat(db): drizzle client |
| 12 | Schema：users / sessions / invite_codes | feat(db): auth tables |
| 13 | Schema：works | feat(db): works table |
| 14 | Schema：tags / work_tags | feat(db): tag tables |
| 15 | Schema：likes / favorites + barrel | feat(db): interaction tables |
| 16 | 生成首版 migration | feat(db): initial migration |
| 17 | shadcn/ui init + cn 工具 | chore: shadcn init |
| 18 | Feature 目录占位 | chore: feature dirs |
| 19 | Scripts 收口 + README | docs: readme + scripts |
| 20 | 验收清单走一遍 | — |

---

## Task 1: 初始化仓库与基础配置

**Files:**
- Create: `package.json`
- Create: `.gitignore`
- Create: `tsconfig.json`
- Create: `next.config.mjs`
- Create: `.gitattributes`

- [ ] **Step 1.1: 初始化 git**

```bash
cd /d/App/Web/Aircade
git init -b main
```

Expected: `Initialized empty Git repository`

- [ ] **Step 1.2: 创建 .gitignore**

Create `.gitignore`:

```gitignore
# deps
node_modules
.pnpm-store

# next
.next
out

# env
.env
.env.local
.env.*.local
!.env.example

# vitest/coverage
coverage
.vitest

# logs
*.log

# IDE
.idea
.vscode/*
!.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# drizzle meta cache (migrations themselves are committed)
drizzle/.cache

# public/uploads runtime
public/uploads
```

- [ ] **Step 1.3: 创建 .gitattributes**

Create `.gitattributes`:

```
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
```

- [ ] **Step 1.4: 初始化 package.json**

Create `package.json`:

```json
{
  "name": "aircade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:gen": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  }
}
```

- [ ] **Step 1.5: 创建 tsconfig.json**

Create `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules", "drizzle"]
}
```

- [ ] **Step 1.6: 创建 next.config.mjs**

Create `next.config.mjs`:

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    remotePatterns: [],
  },
  experimental: {
    // 暂无
  },
};

export default nextConfig;
```

- [ ] **Step 1.7: 提交**

```bash
git add .gitignore .gitattributes package.json tsconfig.json next.config.mjs
git commit -m "chore: init repo baseline"
```

---

## Task 2: 安装核心依赖 + Tailwind/PostCSS baseline

**Files:**
- Modify: `package.json`（pnpm 会写入 dependencies 区块）
- Create: `postcss.config.mjs`
- Create: `tailwind.config.ts`（最小版，Task 3 会扩展）
- Create: `src/app/globals.css`

- [ ] **Step 2.1: 安装运行时依赖**

```bash
pnpm add next@^14.2 react@^18.3 react-dom@^18.3
pnpm add drizzle-orm@^0.30 postgres@^3.4 ioredis@^5.4
pnpm add bcryptjs@^2.4 jose@^5.6 zod@^3.23
pnpm add clsx@^2.1 tailwind-merge@^2.3 class-variance-authority@^0.7 lucide-react@^0.400
```

Expected: `package.json` 更新，无 peer 警告致命错误（警告不算错）。

- [ ] **Step 2.2: 安装开发依赖**

```bash
pnpm add -D typescript@^5.4 @types/node @types/react @types/react-dom @types/bcryptjs
pnpm add -D tailwindcss@^3.4 postcss@^8.4 autoprefixer@^10.4
pnpm add -D drizzle-kit@^0.21
pnpm add -D eslint@^8.57 eslint-config-next@^14.2 prettier@^3.3 prettier-plugin-tailwindcss@^0.6
pnpm add -D husky@^9 lint-staged@^15
pnpm add -D vitest@^2 @vitest/ui@^2
pnpm add -D tsx@^4
```

Expected: `pnpm-lock.yaml` 生成。

- [ ] **Step 2.3: 验证 Typescript 编译器能跑**

```bash
pnpm typecheck
```

Expected: 没有源文件，`tsc` 早退即成功（无输出）。

- [ ] **Step 2.4: 创建 postcss.config.mjs**

Create `postcss.config.mjs`:

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 2.5: 创建 tailwind.config.ts（最小版）**

Create `tailwind.config.ts`:

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 2.6: 创建 src/app/globals.css（最小版）**

Create `src/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 2.7: 提交**

```bash
git add package.json pnpm-lock.yaml postcss.config.mjs tailwind.config.ts src/app/globals.css
git commit -m "chore: install deps and tailwind baseline"
```

---

## Task 3: Tailwind 主题 token（PRD §8）

**Files:**
- Modify: `tailwind.config.ts`

- [ ] **Step 3.1: 扩展 tailwind 主题**

Replace `tailwind.config.ts` with:

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        brand: {
          orange: '#FF9F6B',
          cream: '#FFE9B8',
          mint: '#9FE3C9',
          pink: '#FFB7C5',
          milk: '#FFFBF5',
          coffee: '#3D2E1F',
        },
      },
      borderRadius: {
        btn: '12px',
        card: '20px',
        input: '10px',
      },
      fontFamily: {
        sans: [
          'var(--font-inter)',
          'var(--font-harmony)',
          'system-ui',
          '-apple-system',
          'sans-serif',
        ],
        display: [
          'var(--font-lxgw)',
          'var(--font-harmony)',
          'var(--font-inter)',
          'sans-serif',
        ],
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 3.2: 运行 typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 3.3: 提交**

```bash
git add tailwind.config.ts
git commit -m "feat(theme): apply PRD brand tokens"
```

---

## Task 4: 字体（next/font）

**Files:**
- Create: `src/app/fonts.ts`
- Create: `public/fonts/.gitkeep`
- Modify: `README.md`（暂未存在，Task 19 写，此处只占位）

- [ ] **Step 4.1: 创建字体占位目录**

```bash
mkdir -p public/fonts
```

Create `public/fonts/.gitkeep`:

```
# 放置自托管字体文件：
# - HarmonyOS_Sans_SC_Regular.woff2（华为 HarmonyOS Sans 简中）
# - LXGWWenKai-Regular.woff2（霞鹜文楷）
# 文件缺失时 fonts.ts 会让 next/font 构建报错；Task 4 的处理办法是：
# 本期只接入 Inter（Google Fonts），Harmony/LXGW 的 next/font/local
# 调用留在 fonts.ts 里但注释掉，等你放好 woff2 再启用。
```

- [ ] **Step 4.2: 创建 src/app/fonts.ts**

Create `src/app/fonts.ts`:

```ts
import { Inter } from 'next/font/google';
// 需要你在 public/fonts 下放置自托管字体后，取消下面两段注释：
// import localFont from 'next/font/local';

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

// export const harmony = localFont({
//   src: '../../public/fonts/HarmonyOS_Sans_SC_Regular.woff2',
//   variable: '--font-harmony',
//   display: 'swap',
// });

// export const lxgw = localFont({
//   src: '../../public/fonts/LXGWWenKai-Regular.woff2',
//   variable: '--font-lxgw',
//   display: 'swap',
// });

// 占位变量：在字体接入前，保证 layout 里组合 className 时不报错
export const harmony = { variable: '' } as { variable: string };
export const lxgw = { variable: '' } as { variable: string };
```

- [ ] **Step 4.3: 提交**

```bash
git add public/fonts/.gitkeep src/app/fonts.ts
git commit -m "feat(theme): wire Inter via next/font, placeholders for Harmony/LXGW"
```

---

## Task 5: 根 layout + 占位首页

**Files:**
- Create: `src/app/layout.tsx`
- Create: `src/app/page.tsx`

- [ ] **Step 5.1: 创建根 layout**

Create `src/app/layout.tsx`:

```tsx
import type { Metadata } from 'next';
import { inter, harmony, lxgw } from './fonts';
import './globals.css';

export const metadata: Metadata = {
  title: {
    default: 'Aircade',
    template: '%s · Aircade',
  },
  description: '群友造的街机厅——AI 时代的小作品展示站。',
  openGraph: {
    title: 'Aircade',
    description: '群友造的街机厅',
    siteName: 'Aircade',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Aircade',
    description: '群友造的街机厅',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="zh-CN"
      className={`${inter.variable} ${harmony.variable} ${lxgw.variable}`}
    >
      <body className="min-h-screen bg-brand-milk font-sans text-brand-coffee antialiased">
        {children}
      </body>
    </html>
  );
}
```

- [ ] **Step 5.2: 创建占位首页**

Create `src/app/page.tsx`:

```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 py-24">
      <h1 className="text-5xl font-bold tracking-tight">Aircade</h1>
      <p className="mt-4 text-lg text-brand-coffee/70">群友造的街机厅</p>
      <button
        type="button"
        className="mt-10 rounded-btn bg-brand-orange px-6 py-3 text-white shadow-sm transition hover:bg-brand-orange/90"
      >
        脚手架已就绪
      </button>
    </main>
  );
}
```

- [ ] **Step 5.3: 启动 dev server 验证**

```bash
pnpm dev
```

Expected: 终端显示 `Local: http://localhost:3000`。手动打开浏览器核对：页面奶白底色、深棕文字、`Aircade` 标题、橙色圆角按钮可见。核对完后 `Ctrl+C` 停止。

- [ ] **Step 5.4: 运行 build**

```bash
pnpm build
```

Expected: `✓ Compiled successfully`，生成 `.next/`。

- [ ] **Step 5.5: 提交**

```bash
git add src/app/layout.tsx src/app/page.tsx
git commit -m "feat(app): baseline layout + placeholder home"
```

---

## Task 6: Env 校验（TDD）

**Files:**
- Create: `.env.example`
- Create: `src/lib/env.ts`
- Create: `src/lib/env.test.ts`

- [ ] **Step 6.1: 创建 .env.example**

Create `.env.example`:

```
# --- Database ---
DATABASE_URL=postgres://aircade:aircade@127.0.0.1:5432/aircade

# --- Redis ---
REDIS_URL=redis://127.0.0.1:6379

# --- Auth ---
AUTH_SECRET=change-me-at-least-32-bytes-long-xxxxxxxxx
AUTH_SESSION_COOKIE=aircade_sid
AUTH_SESSION_TTL_DAYS=30

# --- App ---
NEXT_PUBLIC_SITE_NAME=Aircade
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Uploads (V1.0 本地磁盘, 本期脚手架不使用, 先占位) ---
UPLOAD_DIR=/var/aircade/uploads
UPLOAD_PUBLIC_BASE=/uploads
```

- [ ] **Step 6.2: 写失败测试**

Create `src/lib/env.test.ts`:

```ts
import { describe, it, expect, afterEach } from 'vitest';

const ORIGINAL = { ...process.env };

function resetEnv() {
  for (const key of Object.keys(process.env)) delete process.env[key];
  Object.assign(process.env, ORIGINAL);
}

// 每个 case 用不同的 query 串让 Vite 视作不同模块，绕过 import cache。
function importFresh() {
  return import(`./env?t=${Date.now()}_${Math.random()}`);
}

describe('env', () => {
  afterEach(() => {
    resetEnv();
  });

  it('throws when DATABASE_URL missing', async () => {
    delete process.env.DATABASE_URL;
    process.env.REDIS_URL = 'redis://127.0.0.1:6379';
    process.env.AUTH_SECRET = 'a'.repeat(32);
    await expect(importFresh()).rejects.toThrow(/DATABASE_URL/);
  });

  it('throws when AUTH_SECRET shorter than 32 chars', async () => {
    process.env.DATABASE_URL = 'postgres://u:p@127.0.0.1:5432/db';
    process.env.REDIS_URL = 'redis://127.0.0.1:6379';
    process.env.AUTH_SECRET = 'too-short';
    await expect(importFresh()).rejects.toThrow(/AUTH_SECRET/);
  });

  it('loads ok when all required vars present', async () => {
    process.env.DATABASE_URL = 'postgres://u:p@127.0.0.1:5432/db';
    process.env.REDIS_URL = 'redis://127.0.0.1:6379';
    process.env.AUTH_SECRET = 'a'.repeat(32);
    const mod = await importFresh();
    expect(mod.env.DATABASE_URL).toMatch(/^postgres:\/\//);
    expect(mod.env.AUTH_SESSION_TTL_DAYS).toBe(30);
  });
});
```

- [ ] **Step 6.3: 跑测试确认失败**

先配 vitest（下一步）。此步跳过运行，等 Task 7 的 vitest 接入后回来跑。

> **Note:** 因为 `pnpm test` 需要 vitest 配置先就位，这里把"跑测试"挪到 Task 7 末尾统一做。仍然按 TDD：下一步先写 env.ts，再到 Task 7 结尾跑测试看 PASS。

- [ ] **Step 6.4: 实现 src/lib/env.ts**

Create `src/lib/env.ts`:

```ts
import { z } from 'zod';

const schema = z.object({
  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),

  DATABASE_URL: z.string().url().startsWith('postgres://', {
    message: 'DATABASE_URL must start with postgres://',
  }),
  REDIS_URL: z.string().url().startsWith('redis://', {
    message: 'REDIS_URL must start with redis://',
  }),

  AUTH_SECRET: z
    .string()
    .min(32, 'AUTH_SECRET must be at least 32 chars'),
  AUTH_SESSION_COOKIE: z.string().default('aircade_sid'),
  AUTH_SESSION_TTL_DAYS: z.coerce.number().int().positive().default(30),

  NEXT_PUBLIC_SITE_NAME: z.string().default('Aircade'),
  NEXT_PUBLIC_SITE_URL: z.string().url().default('http://localhost:3000'),

  UPLOAD_DIR: z.string().default('/var/aircade/uploads'),
  UPLOAD_PUBLIC_BASE: z.string().default('/uploads'),
});

const parsed = schema.safeParse(process.env);

if (!parsed.success) {
  const issues = parsed.error.issues
    .map((i) => `  - ${i.path.join('.')}: ${i.message}`)
    .join('\n');
  throw new Error(
    `[env] Invalid or missing environment variables:\n${issues}\n` +
      `See .env.example for the full list.`
  );
}

export const env = Object.freeze(parsed.data);
export type Env = typeof env;
```

- [ ] **Step 6.5: 提交**

```bash
git add .env.example src/lib/env.ts src/lib/env.test.ts
git commit -m "feat(lib): env validation via zod"
```

---

## Task 7: Vitest 接入

**Files:**
- Create: `vitest.config.ts`

- [ ] **Step 7.1: 创建 vitest.config.ts**

Create `vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const dirname = path.dirname(fileURLToPath(import.meta.url));

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
    globals: false,
    reporters: 'default',
    clearMocks: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(dirname, 'src'),
    },
  },
});
```

- [ ] **Step 7.2: 跑 env 测试（验证 Task 6 的 TDD 用例）**

```bash
pnpm test
```

Expected: 3 个测试全部 PASS，形如：
```
 ✓ src/lib/env.test.ts (3)
Test Files  1 passed (1)
     Tests  3 passed (3)
```

- [ ] **Step 7.3: 提交**

```bash
git add vitest.config.ts
git commit -m "chore: vitest setup"
```

---

## Task 8: ESLint + Prettier

**Files:**
- Create: `.eslintrc.cjs`
- Create: `.eslintignore`
- Create: `.prettierrc`
- Create: `.prettierignore`

- [ ] **Step 8.1: 创建 .eslintrc.cjs**

Create `.eslintrc.cjs`:

```js
/** @type {import('eslint').Linter.Config} */
module.exports = {
  root: true,
  extends: ['next/core-web-vitals'],
  rules: {
    // 禁止直接读 process.env，强制通过 src/lib/env.ts
    'no-restricted-globals': 'off',
    'no-process-env': 'error',

    // 禁止跨 feature 相互 import，强制通过 src/shared 共享
    'no-restricted-imports': [
      'error',
      {
        patterns: [
          {
            group: ['@/features/*/*', '!@/features/*/index', '!@/features/*'],
            message:
              'Cross-feature imports must go through the feature index. Put shared logic in src/shared.',
          },
        ],
      },
    ],
  },
  overrides: [
    {
      files: ['src/lib/env.ts'],
      rules: { 'no-process-env': 'off' },
    },
    {
      files: ['**/*.test.ts', '**/*.test.tsx'],
      rules: { 'no-process-env': 'off' },
    },
    {
      files: ['drizzle.config.ts', 'vitest.config.ts', 'next.config.mjs'],
      rules: { 'no-process-env': 'off' },
    },
  ],
};
```

- [ ] **Step 8.2: 创建 .eslintignore**

Create `.eslintignore`:

```
node_modules
.next
out
coverage
drizzle
public/fonts
pnpm-lock.yaml
```

- [ ] **Step 8.3: 创建 .prettierrc**

Create `.prettierrc`:

```json
{
  "singleQuote": true,
  "semi": true,
  "trailingComma": "es5",
  "printWidth": 80,
  "tabWidth": 2,
  "arrowParens": "always",
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

- [ ] **Step 8.4: 创建 .prettierignore**

Create `.prettierignore`:

```
node_modules
.next
out
coverage
drizzle
public/fonts
pnpm-lock.yaml
*.md
```

- [ ] **Step 8.5: 跑 lint 自检**

```bash
pnpm lint
```

Expected: 无错（可能有 0 warnings）。若报错请按提示修。

- [ ] **Step 8.6: 提交**

```bash
git add .eslintrc.cjs .eslintignore .prettierrc .prettierignore
git commit -m "chore: eslint + prettier config"
```

---

## Task 9: Husky + lint-staged

**Files:**
- Create: `.husky/pre-commit`
- Modify: `package.json`

- [ ] **Step 9.1: 初始化 husky**

```bash
pnpm exec husky init
```

Expected: `.husky/pre-commit` 生成（默认写的是 `pnpm test`，下一步覆盖它）。

- [ ] **Step 9.2: 覆盖 pre-commit 钩子**

Replace `.husky/pre-commit`:

```sh
pnpm exec lint-staged
pnpm typecheck
```

- [ ] **Step 9.3: 在 package.json 加 prepare 脚本和 lint-staged 配置**

Modify `package.json` — 加两块（用 Edit，不要整体重写）：

scripts 块内追加 `"prepare": "husky"`。

根对象追加：

```json
"lint-staged": {
  "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
  "*.{json,css,md,mjs}": ["prettier --write"]
}
```

完整 package.json 在此步后应为：

```json
{
  "name": "aircade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:gen": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,css,md,mjs}": ["prettier --write"]
  },
  "dependencies": { ... 省略 ... },
  "devDependencies": { ... 省略 ... }
}
```

> **Note:** 具体 dependencies/devDependencies 内容以 pnpm 安装后实际写入为准，别手工改。

- [ ] **Step 9.4: 验证 hook**

```bash
echo "// noop" >> src/lib/env.ts
git add src/lib/env.ts
git commit -m "chore: verify hook"
```

Expected: pre-commit 钩子触发，lint-staged 跑过，typecheck 通过，commit 成功。

- [ ] **Step 9.5: 清理验证注释**

```bash
git reset --soft HEAD~1
```

Open `src/lib/env.ts`, remove the trailing `// noop` line.

- [ ] **Step 9.6: 正式提交 hook 配置**

```bash
git add .husky/pre-commit package.json src/lib/env.ts
git commit -m "chore: husky + lint-staged"
```

---

## Task 10: Redis 客户端

**Files:**
- Create: `src/lib/redis.ts`

- [ ] **Step 10.1: 创建 redis 单例**

Create `src/lib/redis.ts`:

```ts
import IORedis, { type Redis } from 'ioredis';
import { env } from './env';

declare global {
  // eslint-disable-next-line no-var
  var __aircade_redis__: Redis | undefined;
}

function createClient(): Redis {
  const client = new IORedis(env.REDIS_URL, {
    lazyConnect: true,
    maxRetriesPerRequest: 3,
    enableReadyCheck: true,
  });
  client.on('error', (err) => {
    // 避免 dev 阶段整进程 crash；生产环境外层会记录
    console.error('[redis] error:', err.message);
  });
  return client;
}

export const redis: Redis =
  globalThis.__aircade_redis__ ?? (globalThis.__aircade_redis__ = createClient());
```

- [ ] **Step 10.2: typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 10.3: 提交**

```bash
git add src/lib/redis.ts
git commit -m "feat(lib): redis singleton"
```

---

## Task 11: Drizzle 配置 + db client

**Files:**
- Create: `drizzle.config.ts`
- Create: `src/db/client.ts`

- [ ] **Step 11.1: 创建 drizzle.config.ts**

Create `drizzle.config.ts`:

```ts
import { defineConfig } from 'drizzle-kit';
import 'dotenv/config';

const url = process.env.DATABASE_URL;
if (!url) {
  throw new Error(
    'DATABASE_URL not set. Copy .env.example to .env.local and fill it in.'
  );
}

export default defineConfig({
  schema: './src/db/schema/index.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: { url },
  strict: true,
  verbose: true,
});
```

- [ ] **Step 11.2: 安装 dotenv（drizzle.config.ts 需要）**

```bash
pnpm add -D dotenv@^16
```

- [ ] **Step 11.3: 创建 db client 单例**

Create `src/db/client.ts`:

```ts
/**
 * Drizzle client — Postgres 连接单例。
 *
 * 此模块使用 postgres.js 驱动，不兼容 Edge runtime。
 * 任何 import 此模块的 Route Handler / Server Component
 * 都应显式运行在 Node.js runtime（Next.js 默认即 Node）。
 */
import { drizzle, type PostgresJsDatabase } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import { env } from '@/lib/env';
import * as schema from './schema';

declare global {
  // eslint-disable-next-line no-var
  var __aircade_pg__: ReturnType<typeof postgres> | undefined;
  // eslint-disable-next-line no-var
  var __aircade_db__: PostgresJsDatabase<typeof schema> | undefined;
}

const client =
  globalThis.__aircade_pg__ ??
  (globalThis.__aircade_pg__ = postgres(env.DATABASE_URL, {
    max: 10,
    idle_timeout: 30,
  }));

export const db: PostgresJsDatabase<typeof schema> =
  globalThis.__aircade_db__ ??
  (globalThis.__aircade_db__ = drizzle(client, { schema }));
```

> **Note:** 这里 import 了 `./schema`，下一组任务会把 `schema/index.ts` 写出来。当前 typecheck 会报"找不到 schema"——这是预期的，Task 12 会解决。暂**不**跑 typecheck。

- [ ] **Step 11.4: 提交**

```bash
git add drizzle.config.ts src/db/client.ts package.json pnpm-lock.yaml
git commit -m "feat(db): drizzle config + client singleton"
```

---

## Task 12: Schema — users / sessions / invite_codes

**Files:**
- Create: `src/db/schema/users.ts`
- Create: `src/db/schema/sessions.ts`
- Create: `src/db/schema/invite-codes.ts`
- Create: `src/db/schema/index.ts`（barrel，仅导 users/sessions/invite_codes；Task 15 会继续追加）

- [ ] **Step 12.1: 创建 invite-codes schema**

> **关于循环 FK：** spec §5.2 要求 `invite_codes.created_by` → `users.id`；同时 `users.invite_code_used` → `invite_codes.id`。如果两个模块都写 `import` + `.references()`，会形成 TS 模块循环依赖。drizzle 的官方解法：先创建两张表且只保留**一个方向**的 FK，另一个方向的 FK 通过后续 migration（或在同一个 migration 里的 `ALTER TABLE`）补上。本期选择让 `users → invite_codes` 的 FK 在 schema 中声明，`invite_codes → users` 的 FK 通过 Task 16 里一个补丁 migration 添加。

Create `src/db/schema/invite-codes.ts`:

```ts
import { sql } from 'drizzle-orm';
import {
  pgTable,
  text,
  varchar,
  timestamp,
  integer,
  uniqueIndex,
  index,
} from 'drizzle-orm/pg-core';

export const inviteCodes = pgTable(
  'invite_codes',
  {
    id: text('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    code: varchar('code', { length: 32 }).notNull(),
    type: text('type', { enum: ['single', 'multi'] })
      .notNull()
      .default('single'),
    maxUses: integer('max_uses').notNull().default(1),
    usedCount: integer('used_count').notNull().default(0),
    // 注：FK → users.id 由 Task 16 的手写 migration 补丁添加，避免 TS 模块循环。
    createdBy: text('created_by').notNull(),
    expiresAt: timestamp('expires_at', { withTimezone: true }),
    status: text('status', { enum: ['active', 'revoked', 'used'] })
      .notNull()
      .default('active'),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    codeUniq: uniqueIndex('idx_invite_codes_code').on(t.code),
    statusIdx: index('idx_invite_codes_status').on(t.status),
  })
);

export type InviteCode = typeof inviteCodes.$inferSelect;
export type NewInviteCode = typeof inviteCodes.$inferInsert;
```

- [ ] **Step 12.2: 创建 users schema**

Create `src/db/schema/users.ts`:

```ts
import { sql } from 'drizzle-orm';
import {
  pgTable,
  text,
  varchar,
  timestamp,
  integer,
  uniqueIndex,
  index,
} from 'drizzle-orm/pg-core';
import { inviteCodes } from './invite-codes';

export const users = pgTable(
  'users',
  {
    id: text('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    username: varchar('username', { length: 20 }).notNull(),
    passwordHash: text('password_hash').notNull(),
    email: varchar('email', { length: 254 }),
    nickname: varchar('nickname', { length: 30 }).notNull(),
    avatarUrl: text('avatar_url'),
    bio: varchar('bio', { length: 100 }),
    slug: varchar('slug', { length: 40 }).notNull(),
    role: text('role', { enum: ['user', 'admin'] })
      .notNull()
      .default('user'),
    status: text('status', { enum: ['active', 'banned'] })
      .notNull()
      .default('active'),
    inviteCodeUsed: text('invite_code_used').references(() => inviteCodes.id, {
      onDelete: 'set null',
    }),
    failedLoginCount: integer('failed_login_count').notNull().default(0),
    lockedUntil: timestamp('locked_until', { withTimezone: true }),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
    lastLoginAt: timestamp('last_login_at', { withTimezone: true }),
  },
  (t) => ({
    usernameUniq: uniqueIndex('idx_users_username').on(t.username),
    slugUniq: uniqueIndex('idx_users_slug').on(t.slug),
    emailUniq: uniqueIndex('idx_users_email')
      .on(t.email)
      .where(sql`${t.email} is not null`),
    statusIdx: index('idx_users_status').on(t.status),
  })
);

export type User = typeof users.$inferSelect;
export type NewUser = typeof users.$inferInsert;
```

- [ ] **Step 12.3: 创建 sessions schema**

Create `src/db/schema/sessions.ts`:

```ts
import { sql } from 'drizzle-orm';
import {
  pgTable,
  text,
  timestamp,
  index,
  customType,
} from 'drizzle-orm/pg-core';
import { users } from './users';

const inet = customType<{ data: string }>({
  dataType() {
    return 'inet';
  },
});

export const sessions = pgTable(
  'sessions',
  {
    id: text('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    userId: text('user_id')
      .notNull()
      .references(() => users.id, { onDelete: 'cascade' }),
    tokenHash: text('token_hash').notNull(),
    expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
    ip: inet('ip'),
    userAgent: text('user_agent'),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    userIdx: index('idx_sessions_user_id').on(t.userId),
    expiresIdx: index('idx_sessions_expires_at').on(t.expiresAt),
  })
);

export type Session = typeof sessions.$inferSelect;
export type NewSession = typeof sessions.$inferInsert;
```

- [ ] **Step 12.4: 创建 schema barrel（临时版，Task 15 会扩展）**

Create `src/db/schema/index.ts`:

```ts
export * from './users';
export * from './sessions';
export * from './invite-codes';
// works / tags / likes / favorites 等会在 Task 13-15 增量追加
```

- [ ] **Step 12.5: typecheck**

```bash
pnpm typecheck
```

Expected: 无错（此时 `src/db/client.ts` 能找到 schema 了）。

- [ ] **Step 12.6: 提交**

```bash
git add src/db/schema/invite-codes.ts src/db/schema/users.ts src/db/schema/sessions.ts src/db/schema/index.ts
git commit -m "feat(db): auth tables (users, sessions, invite_codes)"
```

---

## Task 13: Schema — works

**Files:**
- Create: `src/db/schema/works.ts`
- Modify: `src/db/schema/index.ts`

- [ ] **Step 13.1: 创建 works schema**

Create `src/db/schema/works.ts`:

```ts
import { sql } from 'drizzle-orm';
import {
  pgTable,
  text,
  varchar,
  timestamp,
  integer,
  json,
  index,
  check,
} from 'drizzle-orm/pg-core';
import { users } from './users';

export const works = pgTable(
  'works',
  {
    id: text('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    authorId: text('author_id')
      .notNull()
      .references(() => users.id, { onDelete: 'cascade' }),
    title: varchar('title', { length: 30 }).notNull(),
    type: text('type', {
      enum: ['game', 'tool', 'social', 'ai', 'other'],
    }).notNull(),
    tagline: varchar('tagline', { length: 30 }).notNull(),
    description: varchar('description', { length: 300 }).notNull(),
    coverUrl: text('cover_url').notNull(),
    screenshots: json('screenshots').$type<string[]>().notNull(),
    qrUrl: text('qr_url'),
    webUrl: text('web_url'),
    status: text('status', {
      enum: ['pending', 'live', 'rejected', 'unlisted'],
    })
      .notNull()
      .default('pending'),
    rejectReason: text('reject_reason'),
    likeCount: integer('like_count').notNull().default(0),
    viewCount: integer('view_count').notNull().default(0),
    featuredAt: timestamp('featured_at', { withTimezone: true }),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
    reviewedAt: timestamp('reviewed_at', { withTimezone: true }),
  },
  (t) => ({
    statusCreatedIdx: index('idx_works_status_created_at').on(
      t.status,
      t.createdAt
    ),
    authorIdx: index('idx_works_author').on(t.authorId),
    featuredIdx: index('idx_works_featured_at').on(t.featuredAt),
    actionTargetCk: check(
      'works_action_target_required',
      sql`${t.qrUrl} is not null or ${t.webUrl} is not null`
    ),
  })
);

export type Work = typeof works.$inferSelect;
export type NewWork = typeof works.$inferInsert;
```

- [ ] **Step 13.2: 更新 schema barrel**

Modify `src/db/schema/index.ts`:

```ts
export * from './users';
export * from './sessions';
export * from './invite-codes';
export * from './works';
// tags / likes / favorites 会在 Task 14-15 增量追加
```

- [ ] **Step 13.3: typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 13.4: 提交**

```bash
git add src/db/schema/works.ts src/db/schema/index.ts
git commit -m "feat(db): works table"
```

---

## Task 14: Schema — tags / work_tags

**Files:**
- Create: `src/db/schema/tags.ts`
- Create: `src/db/schema/work-tags.ts`
- Modify: `src/db/schema/index.ts`

- [ ] **Step 14.1: 创建 tags schema**

Create `src/db/schema/tags.ts`:

```ts
import { sql } from 'drizzle-orm';
import {
  pgTable,
  text,
  varchar,
  boolean,
  timestamp,
  uniqueIndex,
} from 'drizzle-orm/pg-core';

export const tags = pgTable(
  'tags',
  {
    id: text('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    name: varchar('name', { length: 20 }).notNull(),
    slug: varchar('slug', { length: 40 }).notNull(),
    preset: boolean('preset').notNull().default(false),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    nameUniq: uniqueIndex('idx_tags_name').on(t.name),
    slugUniq: uniqueIndex('idx_tags_slug').on(t.slug),
  })
);

export type Tag = typeof tags.$inferSelect;
export type NewTag = typeof tags.$inferInsert;
```

- [ ] **Step 14.2: 创建 work_tags schema**

Create `src/db/schema/work-tags.ts`:

```ts
import { pgTable, text, primaryKey } from 'drizzle-orm/pg-core';
import { works } from './works';
import { tags } from './tags';

export const workTags = pgTable(
  'work_tags',
  {
    workId: text('work_id')
      .notNull()
      .references(() => works.id, { onDelete: 'cascade' }),
    tagId: text('tag_id')
      .notNull()
      .references(() => tags.id, { onDelete: 'cascade' }),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.workId, t.tagId] }),
  })
);

export type WorkTag = typeof workTags.$inferSelect;
export type NewWorkTag = typeof workTags.$inferInsert;
```

- [ ] **Step 14.3: 更新 schema barrel**

Modify `src/db/schema/index.ts`:

```ts
export * from './users';
export * from './sessions';
export * from './invite-codes';
export * from './works';
export * from './tags';
export * from './work-tags';
// likes / favorites 会在 Task 15 追加
```

- [ ] **Step 14.4: typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 14.5: 提交**

```bash
git add src/db/schema/tags.ts src/db/schema/work-tags.ts src/db/schema/index.ts
git commit -m "feat(db): tags + work_tags"
```

---

## Task 15: Schema — likes / favorites + barrel 最终版

**Files:**
- Create: `src/db/schema/likes.ts`
- Create: `src/db/schema/favorites.ts`
- Modify: `src/db/schema/index.ts`

- [ ] **Step 15.1: 创建 likes schema**

Create `src/db/schema/likes.ts`:

```ts
import {
  pgTable,
  text,
  timestamp,
  primaryKey,
} from 'drizzle-orm/pg-core';
import { users } from './users';
import { works } from './works';

export const likes = pgTable(
  'likes',
  {
    userId: text('user_id')
      .notNull()
      .references(() => users.id, { onDelete: 'cascade' }),
    workId: text('work_id')
      .notNull()
      .references(() => works.id, { onDelete: 'cascade' }),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.userId, t.workId] }),
  })
);

export type Like = typeof likes.$inferSelect;
export type NewLike = typeof likes.$inferInsert;
```

- [ ] **Step 15.2: 创建 favorites schema**

Create `src/db/schema/favorites.ts`:

```ts
import {
  pgTable,
  text,
  timestamp,
  primaryKey,
} from 'drizzle-orm/pg-core';
import { users } from './users';
import { works } from './works';

export const favorites = pgTable(
  'favorites',
  {
    userId: text('user_id')
      .notNull()
      .references(() => users.id, { onDelete: 'cascade' }),
    workId: text('work_id')
      .notNull()
      .references(() => works.id, { onDelete: 'cascade' }),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.userId, t.workId] }),
  })
);

export type Favorite = typeof favorites.$inferSelect;
export type NewFavorite = typeof favorites.$inferInsert;
```

- [ ] **Step 15.3: 更新 schema barrel（最终版）**

Modify `src/db/schema/index.ts`:

```ts
export * from './users';
export * from './sessions';
export * from './invite-codes';
export * from './works';
export * from './tags';
export * from './work-tags';
export * from './likes';
export * from './favorites';
```

- [ ] **Step 15.4: typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 15.5: 提交**

```bash
git add src/db/schema/likes.ts src/db/schema/favorites.ts src/db/schema/index.ts
git commit -m "feat(db): likes + favorites"
```

---

## Task 16: 生成首版 migration

**Files:**
- Create: `drizzle/0000_*.sql`（文件名由 drizzle-kit 自动生成）
- Create: `drizzle/meta/*`（drizzle-kit 自动生成）
- Precondition: 本机 PostgreSQL 16 已装；`.env.local` 已配 `DATABASE_URL`；数据库 `aircade` 已创建。

- [ ] **Step 16.1: 准备本地数据库**

（若已存在可跳过）运行下列（请按你本机实际用户名/密码调整）：

```bash
psql -U postgres -c "CREATE USER aircade WITH PASSWORD 'aircade';"
psql -U postgres -c "CREATE DATABASE aircade OWNER aircade;"
```

- [ ] **Step 16.2: 准备 .env.local**

```bash
cp .env.example .env.local
```

编辑 `.env.local` 把 `AUTH_SECRET` 改成 ≥32 字节随机串。

- [ ] **Step 16.3: 生成 migration**

```bash
pnpm db:gen
```

Expected: `drizzle/0000_*.sql` 文件生成；控制台打印类似 `✓ 8 tables generated`。

- [ ] **Step 16.4: 追加补丁 migration 补齐 invite_codes.created_by 外键**

Create `drizzle/0001_invite_codes_created_by_fk.sql`:

```sql
-- 补齐 Task 12 中 invite_codes.created_by 的 FK
-- 拆成独立 migration 是因为 users 和 invite_codes 互相引用，
-- drizzle-kit 无法在一个 CREATE TABLE 中同时处理。
ALTER TABLE "invite_codes"
  ADD CONSTRAINT "invite_codes_created_by_users_id_fk"
  FOREIGN KEY ("created_by") REFERENCES "users"("id")
  ON DELETE NO ACTION ON UPDATE NO ACTION;
```

把该文件名加入 `drizzle/meta/_journal.json` 的 `entries` 列表（drizzle 记账用；生成文件的 `_journal.json` 可参照 0000 那条照葫芦画瓢新增一条，`idx` 递增、`tag` 填 `0001_invite_codes_created_by_fk`、`when` 填当前时间戳毫秒数）。

> **风险提示：** 手改 `_journal.json` 是 drizzle 的已知兜底做法。若后续 `pnpm db:gen` 抱怨 drift，说明 journal 写的不对，按报错回退本步，或在 drizzle schema 中把 `createdBy` 先删掉重跑 generate 再加回来。

- [ ] **Step 16.5: 应用 migration**

```bash
pnpm db:migrate
```

Expected: 控制台打印 `✓ Migrations applied successfully`（应用 2 个文件：0000 和 0001）。

- [ ] **Step 16.6: 手动验证表结构**

```bash
pnpm db:studio
```

在浏览器打开 studio，确认以下表存在：`users` `sessions` `invite_codes` `works` `tags` `work_tags` `likes` `favorites`。展开 `works` 表，确认 `qr_url`/`web_url` 为 nullable，表级有 CHECK 约束 `works_action_target_required`。展开 `invite_codes` 表，确认 `created_by` 有到 `users(id)` 的外键。核对后 `Ctrl+C` 停 studio。

- [ ] **Step 16.7: 提交 migration 文件**

```bash
git add drizzle/
git commit -m "feat(db): initial migration with 8 tables"
```

---

## Task 17: shadcn/ui init + cn 工具

**Files:**
- Create: `components.json`
- Create: `src/lib/utils.ts`
- Modify: `src/app/globals.css`

- [ ] **Step 17.1: 初始化 shadcn（非交互模式）**

Create `components.json`:

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

- [ ] **Step 17.2: 创建 cn 工具**

Create `src/lib/utils.ts`:

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 17.3: 更新 globals.css 接入 shadcn CSS 变量（改造为暖色）**

Replace `src/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* 中性色 —— 覆盖 shadcn 默认 neutral 为 Aircade 暖调 */
    --background: 36 100% 98%; /* brand.milk #FFFBF5 */
    --foreground: 30 35% 18%; /* brand.coffee #3D2E1F */

    --card: 36 100% 98%;
    --card-foreground: 30 35% 18%;

    --popover: 36 100% 98%;
    --popover-foreground: 30 35% 18%;

    /* primary = brand.orange #FF9F6B */
    --primary: 20 100% 71%;
    --primary-foreground: 36 100% 98%;

    /* secondary = brand.cream #FFE9B8 */
    --secondary: 42 100% 86%;
    --secondary-foreground: 30 35% 18%;

    --muted: 36 40% 92%;
    --muted-foreground: 30 25% 40%;

    /* accent = brand.mint #9FE3C9 */
    --accent: 155 55% 76%;
    --accent-foreground: 30 35% 18%;

    --destructive: 0 75% 55%;
    --destructive-foreground: 36 100% 98%;

    --border: 30 25% 85%;
    --input: 30 25% 85%;
    --ring: 20 100% 71%;

    --radius: 12px;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  html,
  body {
    @apply bg-background text-foreground;
  }
}
```

- [ ] **Step 17.4: 扩展 tailwind.config.ts 接通 shadcn 变量**

Modify `tailwind.config.ts` — 在 `extend` 中追加 shadcn 需要的 colors：

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        brand: {
          orange: '#FF9F6B',
          cream: '#FFE9B8',
          mint: '#9FE3C9',
          pink: '#FFB7C5',
          milk: '#FFFBF5',
          coffee: '#3D2E1F',
        },
      },
      borderRadius: {
        btn: '12px',
        card: '20px',
        input: '10px',
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      fontFamily: {
        sans: [
          'var(--font-inter)',
          'var(--font-harmony)',
          'system-ui',
          '-apple-system',
          'sans-serif',
        ],
        display: [
          'var(--font-lxgw)',
          'var(--font-harmony)',
          'var(--font-inter)',
          'sans-serif',
        ],
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 17.5: 验证**

```bash
pnpm lint
pnpm typecheck
pnpm build
```

Expected: 全部通过。首页视觉不变（因为 `bg-brand-milk` 仍然生效；多了一套 CSS 变量而已）。

- [ ] **Step 17.6: 提交**

```bash
git add components.json src/lib/utils.ts src/app/globals.css tailwind.config.ts
git commit -m "chore: shadcn/ui init with warm palette"
```

---

## Task 18: Feature 目录占位

**Files:**
- Create: `src/features/auth/.gitkeep`
- Create: `src/features/works/.gitkeep`
- Create: `src/features/admin/.gitkeep`
- Create: `src/shared/.gitkeep`
- Create: `src/components/ui/.gitkeep`

- [ ] **Step 18.1: 创建占位文件**

```bash
mkdir -p src/features/auth src/features/works src/features/admin src/shared src/components/ui
```

Create each with the same content pattern — `src/features/auth/.gitkeep`:

```
# auth feature：登录/注册/session/邀请码核销等，本期不实现。
# 内部可进一步拆 server/ client/ schemas/。
# 对外只允许通过 `@/features/auth` 入口（未来的 index.ts）暴露 API，
# 跨 feature 引用由 ESLint 规则 no-restricted-imports 限制。
```

`src/features/works/.gitkeep`:

```
# works feature：作品浏览/详情/提交，本期不实现。
```

`src/features/admin/.gitkeep`:

```
# admin feature：管理后台（审核/精选/标签/邀请码/用户），本期不实现。
```

`src/shared/.gitkeep`:

```
# shared：跨 feature 的业务逻辑（如分页、限流、通用列表组件），本期不实现。
```

`src/components/ui/.gitkeep`:

```
# shadcn/ui 原子组件生成位置。
# 用法：pnpm dlx shadcn@latest add button card ...
```

- [ ] **Step 18.2: 提交**

```bash
git add src/features src/shared src/components/ui
git commit -m "chore: feature directory placeholders"
```

---

## Task 19: Scripts 收口 + README

**Files:**
- Create: `README.md`

- [ ] **Step 19.1: 创建 README**

Create `README.md`:

````markdown
# Aircade

> 群友造的街机厅——AI 做游戏社群的作品展示站。

完整产品需求见 [`PRD.md`](./PRD.md)。脚手架设计见 [`docs/superpowers/specs/2026-04-17-scaffold-design.md`](./docs/superpowers/specs/2026-04-17-scaffold-design.md)。

## 快速开始

### 前置要求

- Node.js ≥ 20.11
- pnpm ≥ 9（`npm i -g pnpm`）
- PostgreSQL 16（本机原生安装）
- Redis 7（本机原生安装）

### 准备数据库

```bash
psql -U postgres -c "CREATE USER aircade WITH PASSWORD 'aircade';"
psql -U postgres -c "CREATE DATABASE aircade OWNER aircade;"
```

### 准备环境变量

```bash
cp .env.example .env.local
```

然后编辑 `.env.local`：至少把 `AUTH_SECRET` 改成 ≥32 字节的随机串：

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 安装与启动

```bash
pnpm install
pnpm db:migrate      # 建好 PRD §6 的 8 张表
pnpm dev             # http://localhost:3000
```

## 常用脚本

| 命令 | 说明 |
|---|---|
| `pnpm dev` | 启动 Next.js 开发服务器 |
| `pnpm build` | 生产构建 |
| `pnpm start` | 启动生产服务（先 build） |
| `pnpm lint` | ESLint |
| `pnpm typecheck` | TypeScript 无生成检查 |
| `pnpm test` | 跑 Vitest 一次 |
| `pnpm test:watch` | Vitest watch 模式 |
| `pnpm db:gen` | 从 schema 生成 migration 文件 |
| `pnpm db:migrate` | 把 migration 应用到数据库 |
| `pnpm db:studio` | 启动 Drizzle Studio（浏览器查表） |

## 目录结构

```
src/
├── app/              # Next.js App Router
├── components/ui/    # shadcn/ui 原子组件
├── db/
│   ├── client.ts     # Drizzle 单例
│   └── schema/       # 按表拆分的 schema
├── features/         # 按 feature 聚合的业务目录（auth/works/admin）
├── shared/           # 跨 feature 的业务工具
└── lib/              # 无业务通用工具（env、redis、cn）
```

## 已知 TODO

- [ ] `public/fonts/HarmonyOS_Sans_SC_Regular.woff2` —— 自行放置后在 `src/app/fonts.ts` 取消对应 `localFont` 调用的注释
- [ ] `public/fonts/LXGWWenKai-Regular.woff2` —— 同上
- [ ] 业务功能：见 PRD §3.1 MVP 清单，每个模块会有独立的 spec + plan

## 备份、部署

脚手架阶段不涉及。参考 PRD §7.4 / §7.5 的方案，会在独立子项目实施。
````

- [ ] **Step 19.2: 提交**

```bash
git add README.md
git commit -m "docs: readme with local setup guide"
```

---

## Task 20: 验收清单走一遍

逐项核对，任一项不通过则回溯对应 Task 修复。

- [ ] **Step 20.1: 安装检查**

```bash
pnpm install
```

Expected: 无致命错。

- [ ] **Step 20.2: Lint**

```bash
pnpm lint
```

Expected: 无错。

- [ ] **Step 20.3: Typecheck**

```bash
pnpm typecheck
```

Expected: 无错。

- [ ] **Step 20.4: 测试**

```bash
pnpm test
```

Expected: `env.test.ts` 3 个用例 PASS。

- [ ] **Step 20.5: 构建**

```bash
pnpm build
```

Expected: 构建成功，生成 `.next/`。

- [ ] **Step 20.6: Migration 幂等性**

```bash
pnpm db:migrate
```

Expected: 第二次跑时显示 `No migrations to apply` 或同义信息。

- [ ] **Step 20.7: Dev server 视觉检查**

```bash
pnpm dev
```

手动打开 http://localhost:3000 ：
- 背景色为奶白（brand.milk）
- 标题 `Aircade`、副标题 `群友造的街机厅`
- 橙色圆角按钮文案 `脚手架已就绪`

`Ctrl+C` 停止。

- [ ] **Step 20.8: Studio 表结构核对**

```bash
pnpm db:studio
```

在浏览器确认 8 张表结构、外键、索引、CHECK 约束（参考 Task 16 Step 16.5）。

- [ ] **Step 20.9: 全部通过后打标签（可选）**

```bash
git tag -a v0.0.1-scaffold -m "Scaffold complete"
```

---

## Spec 对照

| spec 章节 | 对应 Task |
|---|---|
| §1 目标 | Task 20 验收 |
| §2 技术决策表 | Task 2（依赖）、Task 11（drizzle）、Task 17（shadcn）|
| §3 目录结构 | Task 1 / 5 / 11 / 18 / 19 |
| §4 依赖清单 | Task 2 |
| §5 Drizzle Schema | Task 12–16 |
| §6 环境变量 | Task 6 |
| §7 设计 Token | Task 3 / 17 |
| §8 根布局 + 首页 | Task 5 |
| §9 测试与工具链 | Task 6 / 7 / 8 / 9 |
| §10 README | Task 19 |
| §11 验收标准 | Task 20 |
| §12 风险与留坑 | Task 4（字体留坑）、Task 1（git init 偏离已声明）|
